// Margin sticker configuration — committed to git so Eva sees the same layout.
// After setting up stickers in edit mode, click "Config 복사" to get the JSON,
// then paste it below and commit the file + the images in images/stickers/.
//
// Format:
// window.STICKER_CONFIG = {
//   '1': [  // chapter id
//     { paragraphIndex: 2, side: 'right', file: 'images/stickers/01.jpg', rotation: -3 },
//   ],
// };

window.STICKER_CONFIG = {};
